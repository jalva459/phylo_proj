#install packages and load libraries that you will work with 
install.packages(picante)
library(picante)
#load phylogenetic trees from Desktop
# import abundance data from Murphy paper
# Remember that we have three objects containing the 
# community (comm)
# phylogeny (phy), 
# and trait (traits) data sets.




### I need to conduct the following calculations for each of the trees 
### generated: the complete island phylogeny and the individual ones for 
### each habitat type





# Habitat Type: Complete 50-Ha Plot (WP=Whole Plot)
#calculate Phylogenetic diversity (PD)
  # WP.pd.result <- pd(comm, phy, include.root=TRUE)
  # WP.pd.result


#calculate standardized effect size (SES)
#calculate Mean Phylogenetic Distance (MPD)
#calculate Mean Nearest Taxon Distance (MNTD)
#calculate Net relatedness Index (NRI)

# Habitat Type: Swamp (SW)
#calculate Phylogenetic diversity (PD)
  # SW.pd.result <- pd(comm, phy, include.root=TRUE)
  # SW.pd.result
#calculate standardized effect size (SES)
#calculate Mean Phylogenetic Distance (MPD)
#calculate Mean Nearest Taxon Distance (MNTD)
#calculate Net relatedness Index (NRI)

# Habitat Type: High Plateau (HP)
#calculate Phylogenetic diversity (PD)
  # HP.pd.result <- pd(comm, phy, include.root=TRUE)
  # HP.pd.result
#calculate standardized effect size (SES)
#calculate Mean Phylogenetic Distance (MPD)
#calculate Mean Nearest Taxon Distance (MNTD)
#calculate Net relatedness Index (NRI)

# Habitat Type: Low-Plateau (LP)
#calculate Phylogenetic diversity (PD)
  # LP.pd.result <- pd(comm, phy, include.root=TRUE)
  # LP.pd.result

#calculate standardized effect size (SES)
#calculate Mean Phylogenetic Distance (MPD)
#calculate Mean Nearest Taxon Distance (MNTD)
#calculate Net relatedness Index (NRI)

# Habitat Type: Slope (SL)
#calculate Phylogenetic diversity (PD)
  # SL.pd.result <- pd(comm, phy, include.root=TRUE)
  # SL.pd.result

#calculate standardized effect size (SES)
#calculate Mean Phylogenetic Distance (MPD)
#calculate Mean Nearest Taxon Distance (MNTD)
#calculate Net relatedness Index (NRI)

